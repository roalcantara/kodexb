# Gitleaks ignore file
# Format: [RuleID]:[filePath]
#
# NOTE: All paths below are also covered by .gitleaks.toml [allowlist]
# (the canonical source of truth). These entries exist to suppress
# per-commit baseline findings that bypass the allowlist. If they
# trigger WRN on every commit, the entries are already satisfied by
# .gitleaks.toml and can be removed — the allowlist is sufficient.
#
# Known noise: security test fixtures, dummy token specs, output dirs.
