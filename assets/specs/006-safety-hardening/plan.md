<!-- markdownlint-disable-file -->

# Implementation Plan: Safety hardening

**Branch**: `006-safety-hardening` | **Date**: 2026-06-07 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `assets/specs/006-safety-hardening/spec.md`

## Summary

This feature adds a deterministic security subgate and an emit-time handoff scrub validator to close two high-leverage safety gaps: no automated security checks in spec gate, and unsanitized worker handoff prompts. The implementation introduces `mise run spec security` with two checks (dependency risk, Electrobun surface posture), keeps secret scanning enforced by the standard `hk` Gitleaks builtin, adds structured run events under `tmp/security/`, and integrates `spec handoff-scrub` into `spec handoff-generate` so unsafe prompt bodies are blocked before write or dispatch. `mise run spec ready` now invokes `spec gate` first and then runs readiness-specific validations (catalog/tag checks).

## Feature deltas

| Topic           | Delta                                                                         |
| --------------- | ----------------------------------------------------------------------------- |
| Security gate   | New `spec security` command with deterministic fail policy and strict mode    |
| Handoff         | New `spec handoff-scrub` validation before `tmp/handoffs` write and dispatch  |
| CI/hook         | hk pre-commit changed-files pass + review workflow full diff pass + required `security` branch protection check evidence |
| Governance docs | Constitution and SDD workflow references updated to bind machine checks       |
| Verification    | `spec ready` invokes `spec gate` then runs readiness-specific validations (catalog/tag checks) |

## Technical Context

**Language/Version**: TypeScript on Bun runtime (repository-managed toolchain via mise)

**Primary Dependencies**: Bun stdlib (`Bun.file`, fs/process APIs), TypeBox (`Type`, `Value.Check`), `ts-morph` (for AST parsing), Gitleaks (via `hk` builtin)

**Storage**: Filesystem-based inputs/outputs (`bun.lock`, source files, Electrobun config) plus audit/event output in `tmp/security/<YYYY-MM-DD>/<run_id>.ndjson`

**Testing**: `bun:test` for deterministic script/check specs and perf-harnesses; no Gherkin flows.

**Target Platform**: Local developer shell + CI runners; desktop security posture check targets Electrobun config in repository source

**Project Type**: Governance/tooling feature in the Spec Kit pipeline

**Performance Goals**: Changed-files security run remains fast enough for pre-commit usage (500ms p95); no-op dependency path remains low-latency

**Constraints**: Deterministic checks only (no probabilistic classifiers), no weakening of existing lint/trace/app-quality-gate behavior, maintain FCIS boundaries and TypeBox-only validation posture

**Scale/Scope**: New security check modules plus wiring in workflow scripts, hk, review workflow, and targeted guide/constitution updates

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle                     | Status | Notes                                                                              |
| ----------------------------- | ------ | ---------------------------------------------------------------------------------- |
| I Product identity            | PASS   | No user-facing keyboard UX regression; governance tooling only                     |
| II FCIS                       | PASS   | Tooling logic stays in shell/tooling layer; no core impurity introduced            |
| III Source-of-truth honesty   | PASS   | Security checks report true persistence/security outcomes; no false-success writes |
| IV Type-safe contracts        | PASS   | New schemas and allowlist validation use TypeBox                                   |
| V Test-first and evidence     | PASS   | Requirements map to script specs/perf evidence                                     |
| VI Conventions and simplicity | PASS   | One-artifact files and minimal-change wiring                                       |
| VII Renderer system           | PASS   | Renderer styling/system unaffected                                                 |
| VIII Observability            | PASS   | Structured event emission and stderr fallback align with logging guidance          |
| IX Electrobun security        | PASS   | Machine check enforces sandbox/partition/navigation policy for external views      |

**Post-design re-check**: PASS — design keeps deterministic gate semantics, TypeBox contracts, and explicit evidence pathways. Uses Gitleaks industry-standard tool for raw secrets.

## Project Structure

### Documentation (this feature)

```text
assets/specs/006-safety-hardening/
├── spec.md
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── security_scan.contract.md
│   └── handoff_scrub.contract.md
├── checklists/
│   └── requirements.md
└── tasks.md                # generated by /speckit-tasks
```

### Source Code (repository root)

```text
tools/governance/security/
├── scan.script.ts
├── scan.script.spec.ts
├── handoff_scrub.script.ts
├── handoff_scrub.script.spec.ts
├── checks/
│   ├── dependencies.script.ts
│   └── electrobun_surface.script.ts
└── fixtures/

tools/bin/spec.script.ts

.github/workflows/
└── review.yml

assets/guides/
├── SDD_WORKFLOW_GUIDE.md
└── CI_GUIDE.md

.specify/memory/
└── constitution.md
```

**Structure Decision**: Single-project governance path under existing tooling directories, with no new runtime service surface. Raw secrets delegated to `hk` Gitleaks builtin.

## E2e traceability

| Requirement | Feature file | Scenario | Notes                                                                                      |
| ----------- | ------------ | -------- | ------------------------------------------------------------------------------------------ |
| SH-1..SH-12 | None         | None     | This feature is release-gated by deterministic script/test evidence, not new Gherkin flows |

Normative Gherkin remains optional and is intentionally deferred for this tooling-focused increment.

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
| --------- | ---------- | ------------------------------------ |
| None      | N/A        | N/A                                  |
