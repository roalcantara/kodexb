<!-- markdownlint-disable-file -->

# 002 — task source truthfulness (pending)

**Status:** Blocked until **001-sync-frecency-persistence** is merged to `main`.

**Workflow:** kb-slice (checklist → analyze → tasks → implement).

**Scaffold:**

```bash
mise run spec worktree add -- 002-task-source-truthfulness
mise run spec feature-init -- --id 002 --slug task-source-truthfulness
```

Legacy parity: `assets/docs/specs/MILESTONE_02/02_task-source-truthfulness/`
