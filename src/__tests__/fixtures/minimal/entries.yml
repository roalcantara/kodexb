bookmarks:
  https://example.com:
    desc: Example website
    tags: [example, web]

commands:
  git status:
    desc: Show the working tree status
    tags: [git, vcs]

cheats:
  Git basics:
    desc: Common git commands
    tags: [git]
    notes:
      - md: |
          - `git status` — show changes
          - `git add .` — stage all
          - `git commit -m "msg"` — commit

tasks:
  Setup project:
    desc: Initialize the new project structure
    tags: [dev]
    priority: high
    status: todo
