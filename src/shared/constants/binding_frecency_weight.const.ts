/** Frecency bump when opening binding detail (primary navigation). */
export const BINDING_FRECENCY_WEIGHT_PRIMARY = 1

/** Frecency bump when revealing source without full detail navigation. */
export const BINDING_FRECENCY_WEIGHT_REVEAL = 0.5
