/** Shared overlay panel width — command palette, sync modal, task sheet. */
export const OVERLAY_SHELL_WIDTH_PX = 560

/** Main list shell min-width (Raycast-like). */
export const APP_LIST_MIN_WIDTH_PX = 740
