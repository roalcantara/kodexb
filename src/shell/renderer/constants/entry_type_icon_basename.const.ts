/** Default `assets/images/<name>.svg` basenames when no tag-brand icon applies. */
export const ENTRY_TYPE_DEFAULT_SVG_BASENAME = {
  bookmark: 'bookmark',
  cheat: 'markdown',
  command: 'terminal',
  shortcut: 'shortcut',
  task: 'checklist'
} as const
