/** Gap between toolbar filter button and floating dropdown (px). */
export const FILTER_DROPDOWN_GAP_PX = 4

/** Max width of the filter dropdown panel (px). */
export const FILTER_DROPDOWN_MAX_WIDTH_PX = 420

/** Min width for compact (portal) filter panel when anchor is very narrow (px). */
export const FILTER_DROPDOWN_PORTAL_MIN_WIDTH_PX = 280

/** Max height of the scrollable compact filter panel (px). */
export const FILTER_DROPDOWN_PORTAL_MAX_PANEL_HEIGHT_PX = 520

/** Min height of the compact filter panel so the shell stays usable (px). */
export const FILTER_DROPDOWN_PORTAL_MIN_PANEL_HEIGHT_PX = 120

/** Bottom margin when fitting compact filter height to the viewport (px). */
export const FILTER_DROPDOWN_PORTAL_VIEWPORT_BOTTOM_MARGIN_PX = 16

/** Left/right margin when clamping the compact filter panel to the viewport (px). */
export const FILTER_DROPDOWN_PORTAL_VIEWPORT_HORIZONTAL_MARGIN_PX = 12

/** Fallback viewport width when `window` is unavailable (SSR / tests without layout). */
export const FILTER_DROPDOWN_PORTAL_FALLBACK_VIEWPORT_WIDTH_PX = 1024

/** Fallback viewport height when `window` is unavailable (SSR / tests without layout). */
export const FILTER_DROPDOWN_PORTAL_FALLBACK_VIEWPORT_HEIGHT_PX = 768
