# Spec Kit workflow catalog — default upstream SDD cycle + kb workflows.
# Register: specify workflow add .specify/workflows/<id>/workflow.yml

catalogs:
  - name: default
    workflows:
      - path: workflows/speckit/workflow.yml
      - path: workflows/orchestrated-handoff/workflow.yml
