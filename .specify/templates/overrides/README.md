# Spec Kit template overrides

Files here replace core [`.specify/templates/`](../) entries (highest precedence in the
[preset resolution stack](https://github.com/github/spec-kit/blob/main/docs/reference/presets.md)).

kb uses **overrides** instead of catalog **presets** for repo-specific templates. Add
only files that differ from upstream Spec Kit core.
